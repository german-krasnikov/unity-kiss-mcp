# V2 — Version & OS Specificity Verification

## Verdict: PASS-WITH-CORRECTIONS

27 sources re-fetched against ~27 load-bearing claims. All five "known traps" verified as correctly handled by the reports. 3 genuine version-range errors found (1 hard, 2 citation-coverage), 3 minor wording/scope softenings. No OS-scope fabrications; R23 coverage is solid.

## Version-range corrections (claim → cited range → verified range → action)

**Errors requiring fixes:**

1. `EditorApplication.focusChanged` (T5 line 23) → cited `U2022.1–U6000.x` → **verified U2022.2–U6000.x** (fetched: 2021.3 page 404, 2022.1 page 404, 2022.2 page exists with matching wording) → ACTION: change lower bound to 2022.2.
2. "Refresh = sync for imports, async for compilation" (T6 line 34) → cited `[U2021–U6000 | HIGH]` → **wording verified ONLY in 6000.x docs** (fetched: present 6000.0, absent 2021.3, absent 2022.3 — exactly as T1 lines 5–6 correctly state) → ACTION: T6 must retag to U6000.0+ for the doc-wording; pre-6000 behavior is inference. Cross-report inconsistency: T1 is right, T6 overreaches.
3. `UnitySynchronizationContext` overwrite claim (T10 line 34) → cited `[U2021.3–U6000]` against `6000.0/Manual/async-awaitable-continuations.html` (a Unity-6-only page; T3 line 33 tags the same URL `U6000.0+`, correctly) → ACTION: either retag T10 to U6000.0+ or add a 2021.3-era citation for the SynchronizationContext behavior; current citation does not cover 2021.3.

**Trap re-verification — all confirmed correct as written:**

4. `kAutoRefresh`→`kAutoRefreshMode` (T5): UnityCsReference 2021.2 branch = bool `kAutoRefresh` ONLY, no enum; 2021.3 branch = int `kAutoRefreshMode` + `AssetPipelineAutoRefreshMode{0,1,2}` + legacy-bool fallback; issuetracker "Recompile After Finished Playing gone" found-in 2021.3.10f1/2022.2.0b8/2023.1.0a12 corroborates the ~.10f1 mid-LTS landing. T5's NOT-2021.2 framing is correct.
5. `AssetDatabase.ScheduleRefresh` 2022-only (T1): 2022.3 page exists with the deferred-tick/import-twice wording; 2021.3 AND 6000.0 both 404. Confirmed.
6. Directory Monitoring Windows-only (T4/T5/T11): 2021.3 manual "(available only on Windows)", 6000.0 manual "(Windows only)", CsReference `RuntimePlatform.WindowsEditor` gate + "currently only available on windows". Span 2021.3→6000.x justified.
7. Conditional reload wording (T2/T9): "if the compilation was successful, the [Unity] Editor reloads all assemblies" verbatim on BOTH 2021.3 and 6000.3 RequestScriptCompilation pages. Range justified. (2019.3 page lacks the conditional — outside target range, T2's MED inference acceptable.)
8. Linux regressions (T11): UUM-133944 repro 6000.3.6f1/6000.3.8f1/6000.4.0b8/6000.5.0a6, fixed 6000.3.10f1/6000.4.0b10/6000.5.0a8, "Not reproducible on: Windows 11" — matches T11 exactly. UUM-79033 found 2022.3.44f1/6000.0.17f1, fixed 2022.3.55f1/6000.0.36f1, Linux/Ubuntu 22.04, ulimit-hard-limit trigger — matches.

**Other spot-checks — confirmed:** RequestScriptCompilation 2019.3 page exists w/ full-recompile wording (T2); `RequestScriptCompilationOptions` 2021.1 exists / 2020.3 404 (T2); `assemblyCompilationNotRequired` 2021.3 404 (T2); AssemblyBuilder Obsolete notice on 2023.1 page verbatim (T2); ImportAssetOptions exactly 6 members on 2021.3 page (T1); DisallowAutoRefresh "always performs a refresh… regardless" verbatim on 2021.3 page (T5/T6); UUM-40547 repro 2021.3.27f1/2022.3.3f1/6000.0.0b11, fixed 2021.3.44f1 (T6); ConfigurableEnterPlayModeDetails exists in 2021.3 with same event order (T3); single-instance batch-mode sentence verbatim in 2021.3 CLI docs + `-accept-apiupdate` warning (T7); SafeMode managed-code-never-runs + batch-mode `-ignoreCompilerErrors` verbatim (T10); "implicitly triggers an asset garbage collection" present in both 2021.3 and 6000.0 (T1).

**Minor wording softenings:** (a) T11(d) "always forward slashes" for `file:` paths — docs also permit escaped backslashes on Windows ("you can also use backslashes, but only if you escape each one"); soften to "forward slashes preferred". (b) T11(b) Wayland — docs say "experimental stage" with GPU-vendor constraints (AMD on 22.04; AMD+Nvidia≥550 on 24.04); add "experimental".

## OS-scope audit (claim → declared OS scope → verified scope → action)

| Claim | Declared | Verified | Action |
|---|---|---|---|
| Directory Monitoring (T4/T5/T11) | Windows-only | Confirmed in 2021.3+6000.0 docs + source platform gate | none |
| "macOS has no OS-level file watching" (T4 line 8) | macOS | Docs only say the feature is Windows-only; the broader negative is inference | mark as inference, not HIGH doc fact |
| Linux = no Directory Monitoring (T11 line 15) | Linux | Inference, already flagged as such | none |
| Editor.log / upm.log paths (T11) | mac/Win/Linux each | All six paths verbatim on log-files.html incl. Windows SYSTEM-account upm.log | none |
| Linux FS case-sensitive; Wayland (T11) | Linux | Verbatim on 6000.4 system-requirements | add "experimental" to Wayland |
| SO_EXCLUSIVEADDRUSE semantics (T11) | Windows; Mono/Unix flagged NOT FOUND | MS-source scoped; gap honestly flagged | none |
| #1173 socket-release race (T8/T11) | Windows | Issue is Windows-specific per thread | none |
| Mission Control focus-steal (T5) | macOS | Issue title macOS-scoped | none |
| 140-char package path cap (T11) | Windows | MAX_PATH-justified | none |
| APFS default case-insensitive (T11) | macOS | Apple doc | none |
| Auto-refresh focus gate (T5/T10) | universal | Correct — applies to all 3 OSes (Dir-Monitoring is detection-only; no-background-import NOT FOUND properly flagged) | none |

**Silent-macOS-assumption scan:** none found presented as universal — T4 verdict §3, T5 bottom line, and T10's header all explicitly scope their macOS framing. Gap: T7's headless recipe (line 16) shows only the macOS binary path; T11(e) has the Win/Linux paths — canonical doc should merge them. T9's Editor.log reload markers need a pointer to T11's per-OS paths + `-logFile` override caveat.

## Provisional list (claim → why provisional → empirical test to settle)

1. **`[OnCodeDeinitializing]`/`[OnCodeUnloading]`/`[OnCodeLoaded]`/`[OnCodeInitializing]` (T3)** → 6000.5 docs only; 6000.3 page 404s; target is 6000.3.0b7 → tag `provisional / NOT on target`. Test: reference the attribute in a throwaway editor script on 6000.3.0b7 → expect CS0246; keep `beforeAssemblyReload`/`[InitializeOnLoad]` as the canonical hooks.
2. **UUM-133944 applicability to 6000.3.0b7 (T11)** → repro list starts at 6000.3.6f1; beta b7 status unknown (Linux-only anyway) → Test (Linux): 10× cycles of external .cs edit → focus editor (Auto Refresh=Enabled) → assert recompile each cycle.
3. **IN-93874 CleanBuildCache no-op (T2)** → single community report on 6000.0, no public tracker entry → Test on 6000.3.0b7: `RequestScriptCompilation(CleanBuildCache)` with zero dirty scripts; assert `assemblyCompilationFinished` fires (not `assemblyCompilationNotRequired`).
4. **UUM-40547 fix presence on b7 (T6)** → fixed-in "6000.0.X" is unspecific; repro included 6000.0.0b11 → Test: Auto Refresh=Disabled, touch .cs, refocus editor, assert no compile starts.
5. **All `U6000.3`-tagged doc claims vs beta target** → 6000.3 docs describe final 6000.3.x; b7 may predate late fixes (esp. anything fixed in 6000.3.x f-releases) → Test: one end-to-end smoke on b7 of the canonical handshake (external edit → scripted `Refresh()` → `compilationFinished` → `beforeAssemblyReload` → reconnect after `afterAssemblyReload`) with Editor.log `Begin MonoManager ReloadAssembly` corroboration.
6. **T8 repo claims pinned to `@main`** → moving targets, will drift; not version-verifiable → re-pin citations to commit SHAs when canonizing (V1/editorial action).
